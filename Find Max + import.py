from utilss import find_max

numbers = [1, 2, 4, 8, 3, 12]
maxi = find_max(numbers)
print(maxi)

# utilss =
# def find_max(numbers):
# maxi = numbers[0]
# for number in numbers:
# if number > maxi:
# maxi = number
# return maxi
